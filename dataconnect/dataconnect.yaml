specVersion: "v1"
serviceId: "konyapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "konyapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
